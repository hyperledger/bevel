---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: myingress-default-backend
  name: myingress-default-backend
  namespace: {{ .Values.metadata.namespace }}
spec:
  selector:
    matchLabels:
      run: myingress-default-backend
  template:
    metadata:
      labels:
        run: myingress-default-backend
    spec:
      containers:
      - name: myingress-default-backend
        image: {{ .Values.image.defaultbackend }}
        ports:
        - containerPort: 8080
        resources:
          limits:
            cpu: 10m
            memory: 20Mi

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  labels:
    run: myhaproxy-ingress
  name: myhaproxy-ingress
  namespace: {{ .Values.metadata.namespace }}
spec:
  updateStrategy:
    type: RollingUpdate
  selector:
    matchLabels:
      run: myhaproxy-ingress
  template:
    metadata:
      labels:
        run: myhaproxy-ingress
    spec:
      hostNetwork: true
      serviceAccountName: myingress-controller
      containers:
      - name: myhaproxy-ingress
        image: {{ .Values.image.haproxy }}:{{ .Values.image.version }}
        args:
        - --default-backend-service=$(POD_NAMESPACE)/myingress-default-backend
        - --configmap=$(POD_NAMESPACE)/myhaproxy-ingress
        - --sort-backends
        ports:
        - name: http
          containerPort: 80
        - name: https
          containerPort: 443
        - name: stat
          containerPort: 1936
        livenessProbe:
          httpGet:
            path: /healthz
            port: 10253
        env:
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
              