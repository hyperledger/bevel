---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ .Values.proxy.name }}-id-certs
spec:
  secretName: {{ .Values.proxy.name }}-id-certs
  issuerRef:
    name: letsencrypt-prod
  dnsNames:
  - {{ .Values.proxy.name }}-id-service.{{ .Values.proxy.external_url_suffix }}
